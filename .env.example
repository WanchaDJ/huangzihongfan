WEB_PORT=80
MYSQL_ROOT_PASSWORD=change-this-password

# Usually leave this empty. Set it only when the API is served from another
# public origin, for example: https://api.example.com/api
PUBLIC_API_BASE_URL=
